import { DigitalCardLayout } from 'src/layouts/digital-card';

export default function Layout({ children }: { children: React.ReactNode }) {
  return <DigitalCardLayout>{children}</DigitalCardLayout>;
}
